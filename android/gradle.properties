org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
kotlin.code.style=official

# THE SHIPPED VERSION. Read by `app/build.gradle.kts`; overridable on the command line with
# `-Pgetagrip.versionCode=…`. Play rejects an upload whose code is not strictly greater than
# the last one, so this is the line to bump — and the only one.
getagrip.versionCode=2
getagrip.versionName=1.0.1
