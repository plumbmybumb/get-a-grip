# Robolectric runs against a downloaded android-all jar, and the newest one it ships
# support for lags the platform. `compileSdk` here is 37, which has no such jar — pinning
# the test SDK keeps the store tests independent of how far ahead the compile SDK runs.
# Nothing under test is SDK-conditional: it is Room, SQLite and value folds.
sdk=34
