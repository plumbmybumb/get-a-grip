# iOS 1.2.0 (26) and Android 1.2.0 (15) — 26 September 2026

**Superseded builds:** iOS 1.2.0 (24) was submitted, then removed from review by the
developer the same afternoon, and Android 1.2.0 (14) was uploaded into a production draft
that was never sent. Both were rebuilt so the release carries the new three-page routine
builder (build numbers 26 and 15; version 1.2.0 kept, since neither had shipped).

Built from branch `release/next` at `b8fb8c2` (iOS archive) — not yet merged to `main`,
tagged or pushed. Release notes: [iOS en/fr](../1.2.0/) and
[ANDROID_1.2.0_WHATS_NEW.txt](ANDROID_1.2.0_WHATS_NEW.txt). Scope:
[NEXT_RELEASE.md](../../NEXT_RELEASE.md).

## Verification

- iOS: 898 unit tests green; UI suite 46 run, 2 skipped, green after the reveal/teardown
  fix (WeightUnits 3/3, SessionPeakHands 2/2, MaxesFlow 8/8 re-run on the final code).
- Android: 486 engine + 788 app unit tests green; release APK assembles (R8, lintVital).
- A read-only review of `main..release/next` found one high issue (Back on a later hand
  discarded the finished hand of a critical force test), one medium (Android reacted to a
  gauge drop only while visible) and four low; all fixed with tests.

## iOS

- Archive: `~/Library/Developer/Xcode/Archives/2026-09-26/Get a Grip 1.2.0 (24).xcarchive`
  (1.2.0 / 24, `run.nuri.doigt`, app + widget + watch), Release, from
  `xcodegen generate --spec project.local.yml`.
- **Uploaded 26 Sep 14:55 and SUBMITTED for review 15:06** (App Store Connect version 1.2.0,
  build 24 attached, release automatically after approval). Listing: v5 screenshots on
  iPhone 6.9" and 6.5" (8 each) and iPad 13" (3); watch unchanged; What's New from
  `docs/releases/1.2.0/ios-en-US.txt`; description gained one Benchmarks / critical force
  paragraph; review notes unchanged.
- Upload command used: `xcodebuild -exportArchive -archivePath "<archive>" -exportOptionsPlist
  build/releases/ios-1.2.0-24/UploadOptions.plist -exportPath build/releases/ios-1.2.0-24
  -allowProvisioningUpdates` (destination upload, Production iCloud environment).
- **CloudKit schema deployed to Production on 26 September 2026** (CloudKit Console, team
  N96JH9BCQ9): created `CD_CriticalForceRecord`, and added two fields that had shipped in
  earlier App Store builds but never been deployed — `CD_MaxRecord.CD_sideRaw` (per-hand
  maxes, 5 Sep) and `CD_SessionTemplate.CD_startingHandRaw` (start on the right hand,
  18 Sep). Production rejects records carrying unknown fields, so App Store users' maxes and
  routines most likely stopped syncing from those builds until this deploy. **Every release
  now checks Deploy Schema Changes for a pending diff before upload.**
- Screenshots v5 (8 phone slides incl. critical force, 3 iPad, 5 watch):
  `appstore/out_v5/` outside the repo, upload order in `appstore/SUBMISSION.md`.

## Android

- `android/gradle.properties`: versionCode 14, versionName 1.2.0. Signed by Nuri with the
  private upload script from `main` at 9478b9b: bundle SHA-256
  `f9462ca22cd70153e8d40e92a32f146902fac993e466111bee4e94d3f2e200ec`, upload certificate
  `DA:6D:B7:CC:…:CF:24` (unchanged). **Production release sent for review 26 Sep**, together
  with the store listing update (new description, six new phone screenshots). Play notes: `android/store/listing/en-US/changelogs/14.txt`.
- Play artwork refreshed in `android/store/out/` (six screenshots incl. critical force);
  full description updated.
- Baseline profile re-recorded on the API 36 emulator, including the critical force setup.

## Owed on hardware

Critical force timing on a real Progressor (7:3 beat, tare before each hand); Android
session haptics now that `VIBRATE` is declared; the new Android session screen on a
small phone; Live Activity during a critical force test.
