# Full Fable audit disposition — September 6, 2026

The supplied audit covered private `main` at `5cc5f41`. This ledger checks all 106 findings against the canonical combined app repository at `f09c408` plus the local follow-up fixes. A fixed documentation or test-gap item is not counted as a newly discovered app defect. Hardware claims remain limited to the evidence stated below.

| Outcome | Findings |
| --- | ---: |
| Fixed / addressed | 78 |
| Already resolved | 22 |
| Not a current defect | 4 |
| Deferred | 2 |

These changes have not been published as a store release. See the [review and verification record](BUG_REVIEW_2026-09-06.md) for test results and remaining hardware checks.

## High

| ID | Finding | Outcome and current evidence |
| --- | --- | --- |
| H1 | A mid-session tare restarts the stream but arms no stale-batch heal, freezing the rep clock permanently | **Fixed / addressed**. Both RunnerSession implementations arm bounded single-use stale-batch recovery before the tare stream restart; lifecycle regressions cover tare recovery and refusal. |
| H2 | Reminder suppression removes REPEATING daily requests, so a fully-trained day silences every future day until the app is next opened | **Fixed / addressed**. Both ReminderPlanner implementations preserve future reminder days after today is complete. iOS uses a finite dated queue within its notification budget; Android schedules the next eligible day. |

## Medium

| ID | Finding | Outcome and current evidence |
| --- | --- | --- |
| M1 | The big numeral shows the HOLD length while paused or waiting to let go, not the clock it interrupted | **Already resolved**. Current runner hero uses the frozen interrupted phase while paused, and the release prompt rather than restarting the hold numeral. |
| M2 | The Live Activity's rest countdown never re-anchors, because .releasing and .resting produce an identical push signature | **Fixed / addressed**. Releasing and resting are distinct activity phases; only resting has a countdown, so LET GO re-anchors the real rest interval. |
| M3 | A finished session is dismissed even when its log failed to save, and the reps are then unrecoverable | **Already resolved**. Current save paths retain the summary on failed persistence, allowing retry instead of dismissing the recorded pulls. |
| M4 | The Live Activity is not ended when the session finishes — only when the summary is dismissed | **Fixed / addressed**. Activity/notification ends at engine completion while the workout summary remains open. |
| M5 | A Live Activity left over from a killed process is never cleaned up, so the next session shows two identical cards | **Fixed / addressed**. iOS cleans up captured orphan ActivityKit IDs on launch/new session. Android clears stale notifications on launch and uses a single notification ID. |
| M6 | A gauge-free session does not pause on backgrounding when a Progressor happens to be connected | **Already resolved**. Current background policy checks timer-only sessions independently of any connected Progressor and pauses them. |
| M7 | The last FINGER can be turned off while the thumb stays, producing a thumb-only "grip" | **Fixed / addressed**. Both inline and island/panel finger pickers require at least one non-thumb finger on both platforms; pure selection regressions cover the rule. |
| M8 | CLAUDE.md's grip-picker section describes a palette, tiles and "Something else" that no longer exist | **Not a current defect**. The cited private CLAUDE.md is absent from canonical source; current AGENTS.md and grip editor are authoritative. |
| M9 | CLAUDE.md says a typed value keeps its own dial detent; the code deliberately does the opposite | **Not a current defect**. The cited private CLAUDE.md is absent. Current typed-entry precision and dial behavior are intentional and covered by input regressions. |
| M10 | Tour steps 0–4 carry no tab, so tapping Back from the History step strands the spotlight on the wrong tab | **Fixed / addressed**. Introductory tour steps explicitly target Today so Back from History restores the correct tab and anchors on both platforms. |
| M11 | "Start without a gauge" declares its 44 pt target outside the Button, so the frame and content shape are applied to the wrong view | **Fixed / addressed**. iOS Start without a gauge has its minimum 44-point frame/content shape inside the button label. Android already has a full clickable minimum target. |
| M12 | A session logged for "Yesterday" shows today's date in History's own list | **Fixed / addressed**. Manual-history list rows use the stored training day rather than insertion time on both platforms. |
| M13 | Ending a PAUSED session discards the rep in flight, and can discard the whole session log | **Already resolved**. Current abort path preserves a paused in-flight pull; it no longer loses the rep when ending while paused. |
| M14 | Mock tare offset is measured against a time base the pump then resets to zero, so every demo/simulator session after the first stream reads a large negative load | **Fixed / addressed**. Both mock clients tare against the same profile time base and start a later session unloaded without inheriting the previous session offset. |
| M15 | undoDelete() silently drops isOnDemand — undoing a "Whenever" routine turns it into an owed daily ritual | **Already resolved**. Routine undo already preserves isOnDemand on both platforms, pinned by store regressions. |
| M16 | A one-hand max computes its "old max" from the both-hands fallback, so the rescale offer multiplies typed kg targets by a fabricated ratio | **Fixed / addressed**. Max receipts capture the pre-save MaxTable, compare exact same-side benchmarks and scale shared typed bands only when a valid shared ratio exists. Both platform store regressions cover affected/unaffected hands. |
| M17 | A disconnect after a completed max hides "Use this max", stranding a measurement that is still on screen | **Fixed / addressed**. A completed max result remains usable after disconnect; a new attempt still requires reconnection on both platforms. |

## Low

| ID | Finding | Outcome and current evidence |
| --- | --- | --- |
| L1 | Samples arriving after stopStreaming leave a stale non-zero live reading on screen | **Fixed / addressed**. Both DeviceStore sample handlers reject notifications while !isStreaming; start/stop publish the boundary before calling the transport. DeviceStreamBoundaryTests pins no trailing live value, peak, trace, or runner callback, and acceptance after a new start. |
| L2 | Commands enqueued while disconnected survive into the next physical link, carrying the tare latch with them | **Fixed / addressed**. Both control queues reject commands when the physical transport cannot write; DeviceStore.tare also requires a connected link. Queue and store regressions prove disconnected tare cannot migrate to the next link. |
| L3 | A failed log fetch is read as "no benchmark today", so recordMax can insert a second benchmark WorkoutLog for one day | **Fixed / addressed**. Both TemplateStore.recordMax paths now require a successful history read before inserting a benchmark-day row; Android injected failed-read regression preserves existing marker and still saves max. Concurrent CloudKit devices can still merge day markers; documented rather than adding a risky uniqueness migration. |
| L4 | MaxImpact.PercentMove.id is not unique across identically-named routines | **Fixed / addressed**. PercentMove now carries routineID on both platforms. Swift Identifiable key includes UUID, side and band. Same-name routine regression checks distinct receipt identity. |
| L5 | build.sh run reports a successful launch even when simctl install or launch failed | **Fixed / addressed**. build.sh checks Simulator launch, built bundle identifier, boot completion, install and app launch; it terminates the actual built bundle. Injected open/boot/install/launch/plist failures exit nonzero and never report launch success. |
| L6 | The trace's moving average reaches across a stream gap, contaminating the first point of a resumed run | **Fixed / addressed**. Swift ForceTraceView smoothing is bounded by newest runStart; Android TraceGeometry.smoothed takes the same boundary. Android gap regression proves a 90 kg old run cannot contaminate the first 2 kg point after a gap. |
| L7 | .snappy on the summary's max rows — bounce on something that had no momentum | **Already resolved**. SessionSummaryView max/disclosure updates, IslandHand side swaps and both hold-button release animations use Motion.state(reduceMotion). The remaining linear hold fill measures the actual hold duration. Android counterparts use Motion tokens; no timing/safety semantics changed. |
| L8 | TargetBandRow's collapsed header reads "None" for a set that will actually run the routine's inherited band | **Fixed / addressed**. BuilderDraftPreparation.swift and Android BuilderDraft.editable materialize legacy routine percentages onto otherwise unconfigured sets and clear the local routine fallback. Builder initial and rescued drafts use preparation, so targets are visible/editable/clearable without changing effective loads or storage before Save. BuilderUIAuditTests checks precedence, nil/tiny/per-hand-equivalent max resolution, idempotence, unfinished drafts, and Clear. |
| L9 | UndoBar plays its own success haptic on top of the caller's, so one Undo tap fires two | **Fixed / addressed**. UndoBar.swift no longer owns a success haptic; existing presenting views own Undo feedback, eliminating duplicate feedback on one action. Android uses its existing single snackbar action feedback path. |
| L10 | RepeatingStep's +/− is a raw drag gesture with no accessibility action, so VoiceOver cannot use it | **Already resolved**. RepeatingStep already exposes the button trait and a guarded accessibility action in ValueRow.swift. Android RepeatingStep.kt exposes Role.Button/onClick semantics. Existing pointer and semantics regression coverage remains. |
| L11 | BandTrimmer builds ClosedRanges from unordered bounds and can trap | **Fixed / addressed**. Endpoint clamps already guarded degenerate ranges and percentage scales already include 1 percent. Whole-band movement now uses bounded BandTrimmerMath on both platforms, avoiding inverted ranges for oversized incoming bands while preserving width for normal drags. Tests cover both walls, oversized/zero spans and narrow low-percentage bands. |
| L12 | The live force reading is read in ThresholdGaugeStrip's own body, re-blurring the glass button on every sample | **Already resolved**. FineTuningSection separates ThresholdReadout and ThresholdBar leaf views/composables on both platforms; currentKg changes do not invalidate the static threshold caption or Stop button parent. |
| L13 | GripIslandPanel's header doc describes a white palm and a fullScreenCover; the code draws black and is an overlay | **Fixed / addressed**. GripIslandPanel header now documents the actual black overlay flush to y=0, square top and hidden status bar; retired white-palm design is explicitly historical. Android uses its native overlay and had no conflicting Swift presentation description. |
| L14 | BuilderMode and the seed comment describe a starter-seeded first run; the seed is blank with zero sets | **Already resolved**. Canonical BuilderMode.firstRun and RoutineBuilderView seed comments already describe blank-first with no sets or templates. Android creating modes also seed blank. Private CLAUDE.md references are obsolete and untouched. |
| L15 | The tour's tab switch ignores Reduce Motion | **Already resolved**. Both RootTabView tour tab switches already pass reduceMotion to Motion.state. Android tab selection is an immediate state change rather than an unguarded SwiftUI animation. Simplified the equivalent TourOverlay ternary to Motion.state(reduceMotion). |
| L16 | Today's undo bar is inset twice as far as History's — UndoBar already applies the house margin | **Fixed / addressed**. Removed TodayView extra horizontal UndoBar padding; shared UndoBar remains sole owner of the house margin, matching History. Android snackbar has its own single shared container padding. |
| L17 | "Your sessions will show up here." is shown to a lapsed user, not just a new one | **Fixed / addressed**. Both ConsistencyCards now show first-use copy only when the empty window includes pre-history untracked days; lapsed fully tracked windows no longer imply a new user. Regression cases cover new, lapsed, nonempty and empty input windows. |
| L18 | VoiceOver focus never moves to the tour callout when the step changes | **Fixed / addressed**. iOS TourOverlay now focuses the combined title/body using AccessibilityFocusState after each cancellable step update. Android title/body uses a merged polite live region so step changes are announced without stealing action focus. |
| L19 | The receipt transition ignores Reduce Motion | **Already resolved**. Current MaxEntrySheet reads accessibilityReduceMotion and uses Motion.state(reduceMotion); Android uses shared reduce-motion control behavior. Private audit references superseded code. |
| L20 | The diagnostics Copy button never returns to "Copy", so a second copy gives no feedback | **Fixed / addressed**. iOS already reset Copy after two seconds; now its accessibility value reflects Copied too. Android adds temporary Copied state with a resetting two-second generation task and a polite live region. Existing copy/haptic action retained. |
| L21 | A max delete that the store rolled back leaves the row on screen with no error | **Fixed / addressed**. MaxesView and Android MaxesListScreen retain delete failure state and show a localized inline error when TemplateStore.deleteMax returns false. |
| L22 | Battery percentage truncates on two screens and rounds on a third, so the same reading shows two numbers | **Fixed / addressed**. BatteryDisplay.percentage is the shared bounded rounding policy on each platform. Gauge, Settings, DeviceChip including accessibility, and low-battery routine notes now quote the same integer. Unit tests pin rounding and invalid/bounded inputs. |
| L23 | Skip pull and Skip set are silent no-ops while the session is paused | **Already resolved**. Current RunnerControlPolicy disables skip-pull and skip-set while paused on both platforms, and current runner controls apply the policy. Engine no-op is consistent with visibly unavailable controls. |
| L24 | Losing the link while a paused .releasing is on screen strands the session on LET GO | **Fixed / addressed**. Both SessionRunner.linkLost handlers unwrap paused releasing and replace it with paused resting. Regression proves Resume gives the full rest rather than returning to an unobservable LET GO state. |
| L25 | A degenerate target band makes RepGate.admits an exact Double equality, so the rep can never engage | **Fixed / addressed**. Both pure SessionRunner gate regions now allow +/-0.25 kg engagement tolerance only for equal-endpoint target lines; normal bands unchanged. Zero bands use threshold gate so unloaded zero cannot engage. Independent engine tests cover line, single endpoint, strict normal band, and zero target. |
| L26 | Deduping reminders on save silently drops a routine below its own sessions-a-day count | **Fixed / addressed**. RoutineDraft flags duplicate active reminder times; normalization restores distinct slots when below daily goal, and setSessionsPerDay skips parked/live duplicates before filling defaults. Daily goal is preserved. Both engine regression suites cover normalization and parked collision. |
| L27 | Discovery accepts a control point with only writeWithoutResponse, but the first tare on such a device permanently kills the link | **Fixed / addressed**. Both Progressor discovery paths now require acknowledged write support before claiming connection; tare retains its defensive fail-closed transport check. No ACK-on-send weakening. |
| L28 | StorageMode reports .cloud whenever the container builds, so Settings claims iCloud sync for a signed-out user | **Fixed / addressed**. StorageMode.cloud now explicitly means CloudKit configuration, not proven synchronization. Settings says storage is local, sync depends on signed-in/available iCloud, and completed upload cannot be confirmed. Avoids swapping stores or making CKAccountStatus sound like sync proof. |
| L29 | Midnight rollover depends on unspecified NotificationCenter observer ordering between DayClock and TemplateStore | **Fixed / addressed**. iOS DayClock notifies TemplateStore after changing today; removed independent store time-change observers. New regression advances clock without manually refreshing store. Android already refreshes clock before store through DayClockReceiver. |
| L30 | Nothing sends .resume on returning to .active, so a momentary .inactive strands a session paused | **Not a current defect**. Current timer-only/unsupported-background sessions intentionally require explicit Resume after interruption. Supported connected Progressor sessions continue. Automatic resumption could resume effort while the user is unready; retained deliberate manual-resume policy. |
| L31 | VoiceOver adjusting the target band at either end of the scale silently narrows it | **Already resolved**. BandTrimmer already provides separate Lower/Upper accessibility handles on iOS and corresponding per-handle setProgress semantics on Android. Users can change each end independently; the old shared endpoint-adjust action no longer exists. |
| L32 | Two tour steps point at controls that are conditionally absent, so the spotlight silently disappears | **Fixed / addressed**. iOS Start another now registers the same startButton tour anchor as the primary Start branch. Both platform routine menus register startWithoutGauge as an always-present fallback; the actual disconnected shortcut remains the larger preferred anchor. Android Start another was already anchored. |
| L33 | Today's routine deck animates with bare .snappy and an ad-hoc .easeInOut, not a Motion token | **Already resolved**. TodayView routine-count animation already uses Motion.state(reduceMotion). Android deck uses platform pager/Motion interaction primitives rather than the reported SwiftUI snappy curve. |
| L34 | History's 5-week grid and Today's 14-day strip compute "tracked since" from different inputs and draw the same days differently | **Fixed / addressed**. History DayLedger accepts the stores published trackingSince on both platforms, preserving the same routine-creation origin as Today. Calendar export inherits the same ledger; old log fallback remains for standalone consumers. New ledger regression covers pre-first-log days. |
| L35 | "Try demo mode" is a one-way door — nothing ever calls useMockDevice(false) | **Fixed / addressed**. Added explicit Leave demo mode to disconnected iOS GaugeView. Android GaugeScreen already exposes the exit; selecting a real gauge also replaces the mock in current DeviceStore. |
| L36 | Tare outside the runner writes with no under-load confirmation and no live reading at all | **Fixed / addressed**. iOS GaugeView now uses exact-age Wake/Tare/loaded confirmation and revalidates connection epoch and signed reading. iOS MaxMeasure and Android Gauge/Max controls already applied these checks; left intact. |
| L37 | The max result's dashed rule outlives the curve it is meant to annotate — the .done screen goes blank after six seconds | **Fixed / addressed**. Both max measurement screens capture the completed trace and render it against its frozen newest timestamp with a stable axis. The result remains visible without continuously running a frame loop or observing ongoing force data. |
| L38 | A grip with both a both-hands max and a one-hand max stops displaying its both-hands number | **Fixed / addressed**. Mixed max readouts retain the Both row on both platforms; best numbers are computed separately per displayed hand instead of mixing records. Android regression checks mixed-hand readout data and separate best values. |
| L39 | Test target: main-actor property mutated from nonisolated tearDown | **Fixed / addressed**. TemplateStoreTests uses async tearDown and MainActor.run for scratch-store cleanup; no unsafe isolation override. |

## Notes

| ID | Finding | Outcome and current evidence |
| --- | --- | --- |
| N1 | RepOutcome.earlyRelease is dead and its doc still describes the dropout timeout the app forbids | **Fixed / addressed**. Retained historical earlyRelease serialized case, corrected both engine comments: current runner never ends a rep on dropout timeout; unknown stored outcome is aborted. Historical outcomes remain decodable and exportable. |
| N2 | CLAUDE.md contradicts itself on the audio session's .duckOthers option | **Fixed / addressed**. Canonical repository has no CLAUDE.md. CuePlayer documentation now describes the existing mixWithOthers policy without claiming ducking. |
| N3 | CLAUDE.md still says resolvingTargets is called from RunnerSession.init; nothing in the app calls it | **Fixed / addressed**. Canonical has no CLAUDE.md. Corrected surviving PlanMath/RepSlot comments to frozen MaxTable and per-rep sequence resolution; resolvingTargets explicitly documented as retained set-level utility, not the live session path. |
| N4 | CLAUDE.md's SessionKind listing omits the shipped .benchmark case | **Already resolved**. Canonical has no CLAUDE.md with the stale listing. SessionResult documents shipped benchmark kind and its settlesDay behavior; no false enum listing remains in canonical AGENTS.md. |
| N5 | ProgressorCommand.encoded silently produces an invalid write for addCalibrationPoint | **Fixed / addressed**. ProgressorCommand.encoded returns nil/null for payload-less addCalibrationPoint; both queues refuse that incomplete command and transports unwrap only valid payloads. Existing explicit payload builder remains separate and hardware-unverified. Codec/queue tests pin rejection. |
| N6 | Battery comment states 3.0 V empty while the constant is 3300 mV | **Fixed / addressed**. Swift and Kotlin battery documentation now state 3.3 V empty, matching the existing 3300 mV constant; no battery behavior changed. |
| N7 | Nothing in the tests distinguishes the time-weighted avgKg from a plain mean | **Fixed / addressed**. Paired SessionRunnerEdgeCoverageTests exercise 9 s at 20 kg plus 1 s at 5 kg at different sample cadences (18.5 kg average), and a below-threshold dip excluded from credit (14 kg average). |
| N8 | The gauge-free hold's one-second delta clamp is never exercised | **Fixed / addressed**. Paired timer-only regression feeds consecutive ticks 30 seconds apart without pause and asserts credited hold increases by exactly 1 second. |
| N9 | untargetedGripCount is unreachable, and its consumer's gate can never be true | **Already resolved**. Current missingBenchmarkGripCount filters to percentage-target sets before calling untargetedGripCount, and current Today/builder safeguards use that live path. The helper marks a grip seen only after finding an unresolved set, so an earlier explicit-target set cannot hide a later missing benchmark. Retained live tested helper; UI agent removed the obsolete RoutineWrapUpCard. |
| N10 | TrainingLoad is entirely unreached — the two strain axes are stored and displayed but never folded | **Fixed / addressed**. Both TrainingLoad headers now explicitly say calculations are staged/tested for a future trend, while current screens store and display raw effort axes. Preserved established arithmetic/tests; no unsolicited trend UI. |
| N11 | GripPosition.pinch's comment says the thumb rule is "deliberately not enforced" — it is enforced 70 lines below | **Fixed / addressed**. Swift/Kotlin GripPosition.pinch comments now state the thumb invariant is enforced by GripSpec construction, copying/mutation and decoding. |
| N12 | The DST round-trip test never exercises a zone where local midnight does not exist | **Fixed / addressed**. Added actual missing-midnight Santiago dates 2026-09-06 and 2027-09-05 to independent engine tests on both platforms, asserting round trip and first valid local hour 01:00. |
| N13 | MaxTable.exact and MaxTable.init(keyed:) are unused, and init(keyed:) admits infinity where record refuses it | **Already resolved**. Both keyed MaxTable constructors already filter finite positive values. exact is now actively used by max-impact receipts and related target checks; updated caller comment. Existing validation contracts retained. |
| N14 | The mock never restarts its device-µs epoch, so the store's playback-clock reset and the tare/re-kick recovery paths never run in the Simulator | **Fixed / addressed**. Both mock clients reset deviceMicros on each actual stream start/re-kick, preserving the current scripted raw-load/tare coordinate while already running. Existing repeated-tare regression now verifies restarted epoch 0. |
| N15 | No test exercises LiveProgressorClient at all: the serialized query channel, the reply-timeout poison, the write pacing and the link-local latch wiring are all unpinned | **Fixed / addressed**. Extracted Swift ProgressorControlPointQueue behind an injected transport; actual LiveProgressorClient now delegates to it. Tests pin reply serialization, safety bypass, timeout poison until reconnect, tare latch, stale deadlines, ATT retry/failure, backpressure, disconnected commands, and Stop cancelling deferred Start. Android existing seam extended and covered. |
| N16 | Nothing bounds how long a backgrounded streaming session holds the gauge, so the battery guarantee has an unrecovered hole | **Fixed / addressed**. Both stores remember background state and rearm idle grace when streaming stops while already backgrounded; regression pins this transition. Active supported background workouts remain intentionally unbounded, without adding an arbitrary cutoff that could interrupt legitimate training. |
| N17 | CLAUDE.md states the silence watchdog fires after 1.6 s; the code uses 0.8 s and a 500 ms check interval | **Fixed / addressed**. Updated current RunnerView foreground watchdog comment to 500 ms checks / 0.8 s silence. Current canonical AGENTS.md and BUILDING.md have no retired 1.6 s statement; old private CLAUDE.md is not the current published source. |
| N18 | TarePolicy's doc comment names an isSignalFresh check that confirmationDecision does not take, and DeviceStore claims isSignalFresh drives an overlay no view reads | **Fixed / addressed**. Both TarePolicy comments now describe exact sampleAge revalidation; both stores describe isSignalFresh as a coarse diagnostic/breadcrumb signal rather than a nonexistent overlay dependency. |
| N19 | The gauge-sleep path is unreachable from the app and exists only for a test | **Deferred**. Explicit sleep API remains unreachable from current product UI; no new sleep control added. Removing the existing hardware command lifecycle is unrelated to responsiveness and changes future protocol surface. Queue extraction now makes ACK and ordering behavior testable, but physical sleep/disconnect fallback still requires hardware validation before exposing the feature. |
| N20 | aps-environment is pinned to development in the checked-in entitlement | **Fixed / addressed**. BUILDING.md now requires inspecting APNs production and CloudKit Production entitlements in the final distribution-signed export. Source development defaults remain compatible with local signing; Apple derives signed APNs values from provisioning profiles. |
| N21 | UIBackgroundModes declares remote-notification with no registration or push handling in the app | **Fixed / addressed**. project.yml explicitly documents remote-notification as required by SwiftData CloudKit mirroring, alongside bluetooth-central. Neither capability was removed. |
| N22 | CLAUDE.md's "recordSession is the only way a WorkoutLog is written" no longer matches the store | **Already resolved**. Canonical has no stale CLAUDE.md absolute claim. TemplateStore contains and documents all legitimate WorkoutLog writers: timed sessions, manual logs, and benchmark markers. |
| N23 | CuePlayer.soundEnabled and hapticsEnabled are never written by anything | **Fixed / addressed**. Removed unwired soundEnabled/hapticsEnabled flags and their constant branches/comments from both CuePlayer implementations. No new settings or audio-focus policy changes. |
| N24 | CLAUDE.md's per-hand caption for a typed kilogram band does not exist in the code | **Fixed / addressed**. Removed truncated orphan comments from SetRowView. Actual per-set target controls and captions remain the product behavior. The report requests an edit to private CLAUDE.md, absent from canonical public repo; that stale private documentation is not carried forward. |
| N25 | Dead code in the builder's shared controls: an unused card, an unreachable chip escape hatch and three unused helpers | **Fixed / addressed**. Deleted unused RoutineWrapUpCard.swift, unused IntChipRow otherRange/custom path, unreachable PositionChipRow horizontal/wrap flag and helper, TargetBandRow.percentInt and BandTrimmer Color.isLightColor. Mirrored unreachable chip API/branch removal on Android; active grid/off-menu categorical choices preserved. |
| N26 | tourAnchor's .id(target) claims to be a scroll id nothing scrolls to, and stamps one explicit identity onto many siblings | **Fixed / addressed**. Removed tourAnchor .id(target) and its false scroll-id claim. Geometry preferences remain the spotlight source and allow repeated anchors across cards without assigning duplicate explicit view identity. Android anchor registry never assigned view IDs this way. |
| N27 | CLAUDE.md still says three tabs; RootTabView has four | **Not a current defect**. The cited private CLAUDE.md is absent from the canonical repository; current AGENTS.md has no three-tab constraint and RootTabView exposes the four current tabs. |
| N28 | CLAUDE.md's "THE ROUTINE CHOOSER RAIL HAS NO ScrollView" section describes a control that no longer exists | **Fixed / addressed**. Removed surviving chooser-rail references in RoutineSummary and TemplateStore; Today uses the paged routine deck. The obsolete private CLAUDE.md is not part of the canonical repository. |
| N29 | CapsLabel's doc comment is welded onto ClimbNotch | **Fixed / addressed**. Moved CapsLabel documentation onto CapsLabel instead of ClimbNotch. Android standalone components did not have this misplaced Swift doc block. |
| N30 | The whole Instagram share path is unreachable — instagramAppID is nil | **Fixed / addressed**. Removed unreachable Instagram App ID, direct-share/pasteboard code, alternate button branch, and URL-query scheme. System sharing and Save to Photos remain. Calendar footer no longer advertises the dead path; removed equivalent unused Android constant. |
| N31 | No test covers maxImpact's side-blindness or the oldKg fallback that drives the rescale offer | **Already resolved**. Prior coordinated pass added passing both-platform TemplateStoreTests covering exact-hand max ratio, one-hand rejection, unaffected hand targets, shared fallback rescaling, and mixed exact/fallback rejection. |
| N32 | An unknown RepOutcome raw decodes as .completed, the optimistic direction | **Fixed / addressed**. Both RepSummary decoders fall back to aborted for unreadable/missing outcomes. Independent decode tests cover future string, malformed type, absent outcome, and round trips of every known historical outcome. |
| N33 | HandMode.alternateEachSet's explainer hardcodes "six" regardless of the set's rep count | **Fixed / addressed**. Both HandMode alternateEachSet explainers are count-free: All the pulls on one hand, then all of them on the other, inside one set. Root updated English/French catalog and will regenerate Android localization. |
| N34 | The mock marks every sample as a batch start, so the runner's batch-level stale rejection is never exercised in the Simulator | **Fixed / addressed**. Swift mock now marks only index 0 of each 8-sample notification as isBatchStart; Android already did so. Mock regressions assert exact batch framing on both platforms. |
| N35 | DeviceStore.deinit leaves the background grace task running and the UIKit background assertion unbalanced | **Fixed / addressed**. Swift DeviceStore isolated deinit cancels freshness and background-grace tasks and balances any outstanding UIKit assertion. New lifecycle test releases a store and checks its assertion ends exactly once. Android already owns/cancels lifecycle jobs through its scope. |
| N36 | The write queue has no deadline on an unacknowledged .withResponse write, unlike every other wait in the client | **Fixed / addressed**. Both live transports arm a generation- and entry-ID-guarded 3-second ATT write deadline. Unknown completion clears link-local queue/latch and fails the link without retry, because a late untagged ACK could otherwise acknowledge the wrong entry. Known explicit ATT failures retry once only if not superseded by a newer stream intent. Queue regressions cover both cases and late callbacks. |
| N37 | TemplateStore registers three NotificationCenter observers and never removes them | **Fixed / addressed**. TemplateStore retains remote-change observer token in a deinitializing ownership box; day observers removed in favor of DayClock callback. Weak callback does not retain store. |
| N38 | No UNUserNotificationCenterDelegate, so a reminder that fires while the app is open is silently dropped | **Deferred**. Foreground notification presentation and reminder deep-link routing are optional behavior additions, not required by existing product flow. Blindly forcing banners/sound can interrupt active workouts. Retained system foreground suppression pending an explicit notification policy; background scheduling defects fixed separately. |
| N39 | The "tinted" app-icon palette is not grayscale, contradicting the script's own rule | **Fixed / addressed**. Restored the reviewed procedural icon renderer to scripts/make_app_icon.swift and generated the tinted asset with equal RGB channels. Verified all 1,048,576 pixels are grayscale; the renderer reproduces the existing light icon pixel for pixel. Light/dark geometry is unchanged. |
| N40 | make_app_icon.swift silently writes the LIGHT artwork for any unrecognised style argument | **Fixed / addressed**. The restored icon renderer rejects unknown styles before writing. A darkk invocation exits 64 and creates no file. |
| N41 | The deferred cues.begin() can run after end(), leaving the audio session and haptic engine running for the app's lifetime | **Already resolved**. Current RunnerSession deferred CuePlayer.begin guards !hasEnded before activation; immediate end cannot reactivate cues. Prior lifecycle regressions cover ended session lifecycle. |
| N42 | .task(id: newestTime) is cancelled and re-created on every trace point for the whole session | **Already resolved**. Current Swift ForceTraceView has one long-lived expiry watcher, not a task keyed per newestTime. Frozen max results now also stop this watcher and frame animation. |
| N43 | The Tare button re-renders at sample rate for a reading that never changes what it draws | **Already resolved**. Current runner TareButton renders from phase/coarse isReadingLive and isLoadedForTare state, with raw reading/age checked only in actions. Sample-rate force state is isolated from button rendering on both platforms. |
| N44 | The draft stash is cancel-and-restart per change, which CLAUDE.md explicitly forbids | **Fixed / addressed**. iOS already uses one pending 500 ms task reading the latest draft. Android flow debounce could still defer recovery during a continuous drag; replaced with DraftStashCoalescer, one pending child job per window. Virtual-time test verifies latest continuous input is saved at 500 ms, later windows work, and builder cancellation prevents stale writes. |
| N45 | RoutineSummary.targetMet and TemplateStore.isDoneForToday disagree when sessionsPerDay is 0 | **Fixed / addressed**. Both RoutineSummary.targetMet use completedToday >= max(1,sessionsPerDay), matching store behavior for malformed zero targets. Regression added on both platforms; ordinary normalized writes unchanged. |
| N46 | A gauge-free session is indistinguishable from a measured one that read nothing | **Fixed / addressed**. Both history row subtitles and spoken descriptions omit kg/peak when no positive peak was recorded. Does not label legacy zero rows as no-gauge because stored data cannot prove that mode; no schema migration. |
| N47 | The rendered PNG is written to a fresh temporary directory on every share and never removed | **Fixed / addressed**. Calendar and routine PNG transfers now use named DataRepresentation; CoreTransferable owns file handoff. Analysis CSV uses the same lifetime model. No app-created UUID export directories remain in these paths. SharePNGTests verifies original bytes, PNG type and suggested/exported filename. |
| N48 | IslandHand's hand swap uses ad-hoc easeIn/easeOut curves instead of Motion tokens | **Fixed / addressed**. IslandHand retract and regrow now use Motion.state(reduceMotion); Android already uses shared Motion. Intentional hold-button elapsed-progress fills remain linear. |
