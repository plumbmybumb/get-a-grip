# Get a Grip iOS 1.0.3 / Android 1.0.1 (9) release

Prepared 12 September 2026 from the canonical public checkout.

- iOS: 1.0.3 (7), app and widget.
- Android: 1.0.1 (9), existing closed alpha track.
- Store copy: `releases/1.0.3/`, English and French, separate platform notes.

## Changes

Fix the 20-pull ceiling with a tested 100-pull-per-side limit and bounded builder
previews. Preserve Android workouts and unsaved summary choices through light/dark
mode changes and Activity recreation. Add routine overviews with an Edit action,
History navigation from Last 14 days, the permanent long-rest presentation on both
platforms, and optional open-source community links in Settings.

The Android fix retains in-memory sessions across configuration changes; it does
not promise recovery after process termination. Recipients of shared routines with
more than 20 pulls need this updated version too. No BLE protocol, permissions,
database schema, or signing identity changes are included.

## Validation before packaging

- 687 iOS unit tests and 1,040 Android tests pass.
- Shared replay: 39 scenarios / 4,130 steps; 74 routine shares; 18 export scenarios.
- Focused UI checks cover appearance changes, partial work and unsaved summaries,
  typed 36/100 counts, overview navigation, English/French large text, and rest layout.
- The combined preview is installed and launched on the developer’s iPhone; the
  Android emulator is available for interactive use. This is not a new hardware
  validation claim for any gauge protocol.
- Independent source review found no release blocker. Signed artifact checks and
  store submission results are recorded after packaging.

## Signed artifacts

Both artifacts were built from `0f5263b8973d0d26e9e76d16fe740f80e54b66d0`.
Release tags `ios-v1.0.3-7` and `android-v1.0.1-9` pin that exact source;
subsequent documentation-only commits record store state.

- iOS IPA SHA-256: `b1362dbe3871c062f37da8de4966a07258fc565a41b87d43a16ff0ec1779b921`.
  App/widget identities, version/build and signatures verify. Distribution has
  production APNs and CloudKit entitlements, the existing App Group, and no debugger.
- Android AAB SHA-256: `72294aa9d59ee69d3d1a024ca0467c527a0ea936deafa488524a23288c1087d0`.
  All 122 signed payload entries and the companion APK match the existing upload
  certificate. Native ELF/ZIP alignment and the bundle's 16 KiB generation setting
  pass; mapping and legal resources are present. Permissions and requirements match
  build 8. AGP does not embed a Git SHA for this nested project; external verification
  records the commit and unchanged Android tree instead.
- The initial Android build selected the debug signing fallback. Verification
  rejected it before upload; only the rebuild with the private upload key was used.
- Google reports no supported-device losses. Its optional native debug-symbol
  warning remains for bundled native libraries; the Kotlin/Java ReTrace mapping
  is included.

## Store status

- iOS 1.0.3 (7): **Waiting for Review**, confirmed in App Store Connect on
  12 September 2026 at 13:27 Paris time. Automatic release after approval remains
  selected. Existing listing, screenshots, contact details, ratings and English
  localization are retained; French notes are prepared in the source but no new
  App Store localization was added.
- Android 1.0.1 (9): **Changes in review** after confirming Send changes for review
  on 12 September 2026. Google’s quick checks are still running; the release is
  queued to continue automatically once they pass. English and French notes are
  supplied, with the existing countries, tester group and 100% beta rollout retained.

These statuses do not mean either new build is approved or already available.
